class OrderFinishedController {
  async update(req, res) {
    // Finalizar a Entrega (Atualizar end_date)
    // Ter a funcionalidade de adicionar imagem (signature_id)
    return res.json();
  }
}

export default new OrderFinishedController();
